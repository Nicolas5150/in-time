// Navbar slideout
(function($){
    // Navbar items - mobile theme
    $('.button-collapse').sideNav();
    // Image parallax
    $('.parallax').parallax();
    // Materialize
    $('select').material_select();
})(jQuery);
